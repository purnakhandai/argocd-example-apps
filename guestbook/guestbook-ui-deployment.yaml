apiVersion: apps/v1
kind: Deployment
metadata:
  name: guestbook-library
spec:
  replicas: 3
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: guestbook-library
  template:
    metadata:
      labels:
        app: guestbook-library
    spec:
      containers:
      - image: gcr.io/heptio-images/ks-guestbook-demo:0.2
        name: guestbook-ui
        ports:
        - containerPort: 80
